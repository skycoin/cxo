package skyobject

// TODO (kostyarin): track changes

/*
import (
	"fmt"
	"reflect"
)

func (p *Pack) updateTree() (err error) {
	for i := range p.r.Refs {
		if p.r.Refs[i].walkNode == nil {
			continue
		}
		wn := p.r.Refs[i].walkNode
		if wn.value == nil {
			continue
		}
		if err = p.updateValue(wn.value); err != nil {
			return
		}
		if _, err = p.r.Refs[i].SetValue(wn.value); err != nil {
			return
		}
	}
}

func (p *Pack) updateValue(obj interface{}) (err error) {
	//
}
*/
